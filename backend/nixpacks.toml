[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npx prisma generate", "npx prisma db push", "npm run build"]

[start]
cmd = "npx prisma db seed && npm run start:prod"
